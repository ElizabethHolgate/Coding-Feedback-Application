<% layout('layouts/boilerplate')%>

<h1>All Resources</h1>
    <div class="row">
        <% if(currentUser) { %>
            <% if(currentUser.lecturer == true){ %>
                <div>
                    <a class="btn btn-info" href="/resources/new">Add New Resource</a>
                </div>
            <% } %>
        <% } %>
        <% for (let resource of resources){%>
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title"><%= resource.title %><span class="badge badge-secondary">New</span></h5>
                    <p class="card-text"><%= resource.description %></p>
                    <a href="<%= resource.url %>" class="btn btn-primary" target="_blank">Card link</a>
                    
                </div>
                <span class="badge badge-pill badge-primary">kjaldf</span>
            </div>
        <% } %>
            <div>
            
            <ul>
                <% for (let resource of resources){%>
                    <a href="/resources/<%= resource._id %>"><li> <%= resource.title %>: <%= resource.description %></li></a>
                <% } %>
            </ul>
        </div>
    </div>